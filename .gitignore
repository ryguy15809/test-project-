# Godot 4+ specific ignores
.godot/
/android/

# Build artifacts (Windows .exe only; web/ is committed so the VPS can deploy from git)
/exports/

# Python (auth_service)
__pycache__/
*.pyc
auth_service/*.db
auth_service/*.db-shm
auth_service/*.db-wal
